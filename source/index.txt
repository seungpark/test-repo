:template: product-landing
:hidefeedback: header
:noprevnext:

===========================
DOP-3530
===========================

.. default-domain:: mongodb

Procedure test
--------------

This is a paragraph following the heading, preceding the procedure directive

.. procedure:: 
   :style: normal
   
   .. step:: (Multi Line) On the Connect screen, hover over :guilabel:`Saved Connections` 
      in the left-side navigation and click the :guilabel:`Ellipses (...)` 
      button.

   .. step:: (Single Line) On the Connect screen, hover over :guilabel:`Saved Connections` in the left-side navigation and click the :guilabel:`Ellipses (...)` button.

   .. step:: (Multi Line) Select 
      :guilabel:`Import saved connections`.

   .. step:: (Single Line) Select :guilabel:`Import saved connections`.

   .. step:: (Multi Line) Under :guilabel:`Source File`, 
      click :guilabel:`Select a file`. 

   .. step:: (Single Line) Under :guilabel:`Source File`, click :guilabel:`Select a file`. 


   .. step:: (Multi Line) Select the exported 
      JSON file you want to import.

   .. step:: (Single Line) Select the exported JSON file you want to import.

   .. step::

      (single line on directive content block) Select the exported JSON file you want to import.
